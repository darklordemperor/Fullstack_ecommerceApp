global:
  scrape_interval: 15s
  scrape_timeout: 10s

scrape_configs:
  # Scrapes the RED metrics the backend exposes at /metrics.
  - job_name: ecommerce-backend
    metrics_path: /metrics
    static_configs:
      - targets: ['backend:8080']
